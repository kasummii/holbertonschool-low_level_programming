# Hello World Project

This directory contains a simple C program `4-puts.c` that prints the message:
"Programming is like building a multilingual puzzle" using the puts function.

It compiles without errors or warnings using gcc with flags -Wall -Werror -Wextra -pedantic -std=gnu89.

